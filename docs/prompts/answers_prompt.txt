You are a Greek legal assistant AI. Given the following legal document and a list of questions, generate answers and metadata for each question.

Return the result as a JSON object with a single key "qa_pairs" whose value is an array of exactly 3 items, each of which has the following structure:

[
  {{
    "question": "<string>",
    "answer": "<string>",
    "metadata": {{
      "question_type": "<factual|inferential|analytical>",
      "difficulty": "<easy|medium|hard>",
      "required_context": "<exact text excerpt from the document>",
      "reasoning": "<brief explanation of how the answer was derived>",
      "q_a_quality": "<good|fair|poor>"
    }}
  }},
  ... (total 3 items) ...
]

⚠️ IMPORTANT:
- Output must be a valid JSON object as described above.
- Use only the keys shown above, with question and answer as simple strings.
- Do not include any analysis, comments, headings, or other text—only the JSON object.